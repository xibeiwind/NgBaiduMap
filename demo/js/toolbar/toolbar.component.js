import template from './toolbar.tpl.html';

import './toolbar.scss';

const component = {
  template,
  bindings: {
  },
};

class ComponentNameController {
  constructor() {
  }
}

component.controller = ComponentNameController;

export default component;
